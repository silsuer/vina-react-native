// 便签本主界面的home页面，显示最近消息

import React, { Component } from 'react'
import { View,Text } from 'react-native'

// 最近（任务/日程/便签）界面
export default class RecentView extends Component {
    render(){
        return (
            <View>
                 <Text>这里是RENCENT组件</Text>
            </View>
        )
    }
}